
** PRODUCTION MUST RESOLVE PACKAGE.JSON IN
   SHARED PACKAGE TO DIST **

--- KNOWN ISSUES ---
- Armies with an amount of 1 do not spawn at the barracks after they finish training
- Wrong mail ordering (earlier mails end up at the top)
- Mails do not stop highlighting until "read" marker has reached the server
- Every turn red marker above mail icon refreshes and shows all mails as unread

list of changes to make next:
10. setup a database ( or redis )
12. bug fixes + ui polishment, clean up architecture
13. Music, sound effects, reusable components
14. deploy

--- NON-URGENT TASKS ---
1. Make sure nation flags and assets are pre-loaded so player doesn't wait
for them to load (which can take up to a second)
2. Implement modifier support for other variables
4. Transfer building construction state from hexes into separate objects
5. Create a single function for creating and validating instead of having multiple
sources where its required to validate every time.
6. Reduce amount of building levels on building if right click has happened on hex
7. Instead of transferring hexes directly during war, mark them as occupied (de facto/de jure owner)
and calculate surrender percentage based off of that.
9. Make win/lose menu to and allow player to start new game or continue in spectator
10. Change how paths work to make sure moving goals armies in hex are always reserved no matter the order they run
12. Skip invalid intents that don't pass domain validation. Aim for architecture to be 
client data => copy db data => run logic and update data => if error - log and skip, else update db
13. Update ai road building so it builds towards closest road segment that leads to producing building
14. Enforce AI staging, so it chooses a target hex and prepares for attack instead of attacking the first possible hex.
The flow should be: analyze nation border, score attacks, attack immediately if enough army, else create staging request
and pull army from other hexes to create enough army for attack.
15. Make each building have its own production rates instead of relying on fixed rates for each resource
16. Deprecate world analysis object. Instead invoke every non-expensive selector manually, and keep only expensive
calculcations.
17. Think how AI intents validate after simulation runs.
18. Add mobile support

--- RULES ---
1. Delete/Update first, Build/Add second.

keep in mind that when you apply optimistic update to deleted buildings on client,
a hex that this building belonged to still holds the reference to it.

Resource/Building pipeline:

1. Turns starts, contracts get updated. Client sees optimistic ui of its contracts being working.
new contracts must be working optimsitically, right after creation since player works with previous turn state
2. calcAllocationResource allocates resources using the new updated contracts but prev turn state
3. each building calculates its output based on efficiency which is gathered from allocated resources
4. After every building calculated its new output, we run the allocation again and update each 
building's available resources with new efficiency