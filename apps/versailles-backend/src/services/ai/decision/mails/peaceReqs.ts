import { WorldAnalysis } from "#services/ai/types/analyze.js";
import { AnswerMail, SignPeaceReqIntent } from "#services/ai/types/intent.js";
import { GameCtx } from "#trpc/index.js";
import { Nation } from "@repo/shared";

{
  /* function generateAnswerMailCandidates(
  ctx: GameCtx,
  analysis: WorldAnalysis,
  nation: Nation
): AnswerMail[] {}

function generateSignPeaceReqCandidates(
  ctx: GameCtx,
  analysis: WorldAnalysis,
  nation: Nation
): SignPeaceReqIntent[] {}
 */
}
